- flash.each do |key, value|
  .callout{"data-closable" => "", class: "#{key}"}
    = value
    %button.close-button{"aria-label" => "Dismiss alert", "data-close" => "", :type => "button"}
      %span{"aria-hidden" => "true"} ×